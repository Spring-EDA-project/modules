tasks.named('bootJar'){ // bootJar 세팅을 켜기
    enabled = false
}
dependencies {
    implementation project(':module-core:member-rdbms-core')
    testImplementation platform('org.junit:junit-bom:5.9.1')
    testImplementation 'org.junit.jupiter:junit-jupiter'
}

test {
    useJUnitPlatform()
}